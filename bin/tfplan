#!/usr/bin/env bash

# source tfcommon from same bin folder
. "$(dirname $(realpath $0))/tfcommon"

parse_apply_opts "$@"

echo "Current working directory: $(pwd)"
# echo "Files in current directory: $(ls -la)"
echo "Detected folder ${folder}, env ${env}"

usage() {
  echo "Usage: $0 [folder|folder/env] [--target <resource>] ..."
  echo "Example: $0 aws"
  echo "Example: $0 aws/dev"
  echo "Example: $0 aws/dev --target aws_s3_bucket.example --target aws_iam_role.example"
  exit 1
}

if [[ "$1" == "-h" || "$1" == "--help" ]]; then
  usage
fi

echo "tf plan $folder"

is_tf_folder "$folder" || exit 1

setupworkfolder_cached $folder || { echo "Failed to setup work folder $folder"; exit 1; }

terraform fmt -check -v

terraform plan -input=false $(target_args) -out plan.tfplan | tee >(sed $'s/\033\[[0-9;]*m//g' > plan.txt)
plan_exit=${PIPESTATUS[0]}

target="${folder}${env:+/$env}"
if [[ $plan_exit -eq 0 ]]; then
  echo -e "\e[34mPlan for ${target} complete SUCCESS\e[0m"
else
  echo -e "\e[31mPlan for ${target} FAILED\e[0m"
fi
exit $plan_exit
